# Claude implementation task — Hermes observation service

You own the Hermes side of Issue #81. Implement only the exact cleared contract in `.contract-ref.md` against this Hermes worktree at current main c0106e50e7ecedb3ce34e785d949725dc4e0e457.

Read first:
- repository AGENTS.md / contribution instructions
- `/tmp/ai-country-observation-contract-v1.md`
- existing gateway/API server structure, auth helpers, HTTP client behavior, test conventions, and any deployment/service templates
- upstream PR #83241 only as reference; do not copy generic authority or enable a broad route

Scope:
1. Add a standalone observation service/process/listener, not a profile on the normal listener and not a proxy over `/v1/runs`.
2. V1 exposes only unauthenticated `GET /health` liveness and authenticated `POST /v1/observation` with the exact request/response/error schemas.
3. Enforce authentication before body parsing/target contact; exact route/method/raw-target/header/schema behavior; duplicate JSON key rejection; strict size/time limits; no redirects, DNS, retries, proxy environment, follow-up IDs, or client-selected target data.
4. Use zero MCP servers and no Hermes agent/tool runtime in V1. The fixed action performs one server-owned bounded GET to a literal pinned loopback target and returns only the fixed projection.
5. Add focused adversarial tests for auth ordering, cross-credentials, raw path/header attacks, JSON attacks, target traps/state before-after, redirects, SSRF/target substitution, retries, response identifiers, and bounded logging.
6. Add a non-installed systemd/service-unit template or deployment documentation that expresses the separate OS identity, credential files, filesystem, descriptor, and egress restrictions. This is a template/handoff only, not activation.

Hard constraints:
- no deployment, restart, credential provisioning/rotation, live calls, external network, or secret-file reads;
- no generic Hermes route changes, mutation tool/MCP exposure, session/memory support, terminal, subprocess, delegation, cron, device control, approvals, or public listener;
- do not modify AI Country files from this worktree;
- one writer: only this Claude process edits this worktree.

Verification:
- run focused service tests, compile/lint checks applicable to changed files, and `git diff --check`;
- inspect the final route table and effective tool registry in tests;
- make a local commit only after tests pass, with a clear message;
- return exact commit SHA, changed files, test commands/results, and explicit UNKNOWN items for host/runtime verification. Do not claim deployment or live boundary proof.
